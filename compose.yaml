name: countdown-avatar
services:
  avatar:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - path: .env
        required: false
    environment:
      DATA_DIR: /data
      ADDR: 0.0.0.0:8080
    volumes:
      - ./data:/data
    restart: unless-stopped
